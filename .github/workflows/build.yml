name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS (universal)
            os: macos-14
            cmake_extra: -DARSENAL_UNIVERSAL_BINARY=ON
          - name: Windows (x64)
            os: windows-2022
            cmake_extra: ""
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DARSENAL_COPY_PLUGIN=OFF ${{ matrix.cmake_extra }}

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: arsenal-${{ runner.os }}
          path: |
            build/Arsenal_artefacts/**/VST3/**
            build/Arsenal_artefacts/**/AU/**
            build/Arsenal_artefacts/**/Standalone/**
          if-no-files-found: error
